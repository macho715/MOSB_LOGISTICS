# /kpi:dash

Purpose: Add a KPI dashboard stub (data model + endpoints + UI placeholder).

Outputs (minimal):
- `src/core/kpi.py` (schema + calc stubs)
- `tests/test_kpi.py` (smoke)
- Notes for integrating with WMS/3PL feeds
