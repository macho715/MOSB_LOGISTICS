# /rules:tune

Purpose: Tune Cursor rules for repo reality.

Inputs:
- Repo structure (frontend/backend paths)
- Preferred gates (coverage, lint)

Outputs:
- Updated `.cursor/rules/*.mdc`
- Updated `docs/constitution.md` changelog entry
