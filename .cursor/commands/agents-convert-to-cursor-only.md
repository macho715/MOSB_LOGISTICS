# /agents:convert-to-cursor-only

Purpose: Convert multi-agent repo patterns into Cursor-first rules/commands.

Steps:
1) Consolidate agent guidance into `docs/AGENTS.md`.
2) Bind with `.cursor/rules/016-agents-cursor-only.mdc`.
3) Ensure commands live under `.cursor/commands/*.md`.

Output:
- Cursor-only agent governance enabled.
