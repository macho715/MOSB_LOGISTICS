# /spec:init-constitution

Purpose: Initialize/refresh `docs/constitution.md` and bind it into Cursor rules.

Steps:
1) Ensure `docs/constitution.md` exists (create if missing).
2) Ensure `.cursor/rules/015-constitution-cursor.mdc` is alwaysApply.
3) Add/refresh changelog.

Output:
- Constitution bound and enforced.
